/**
    Write a really clear summary line with an active verb.

    @exports SomeModule

    @requires ARequiredModule
*/
require(
[
    // third party libraries
    'jQuery',

    // standard imports
    'js/constants',

    // other packages
    'view/document',

    // this package
    'model/pageStateModel'
],
function (
        $,
        Const,
        DocView,
        PageStateModel) {


/**
    Write a good function.
*/
function makeGoodDecisions() {
}

/**
    Store important data.
*/
var bigAndImportant = (function () {
    var that = {};

    return that;
}());

return {};


});
